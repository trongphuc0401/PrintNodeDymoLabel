<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PrintNode Status</title>
    <style>
        /* Using similar styles from dashboard.ejs for consistency */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #f4f7f6;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            margin-bottom: 30px;
        }

        .header h1 {
            color: #333;
        }

        .jobs-section {
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        .jobs-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .jobs-table {
            width: 100%;
            border-collapse: collapse;
        }

        .jobs-table th,
        .jobs-table td {
            padding: 15px;
            border-bottom: 1px solid #e9ecef;
            text-align: left;
        }

        .jobs-table th {
            background: #f8f9fa;
            font-weight: 600;
            color: #555;
        }

        .jobs-table tr:hover {
            background: #f8f9fa;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #999;
        }

        .back-link {
            display: inline-block;
            margin-bottom: 20px;
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
        }

        pre {
            background-color: #eee;
            padding: 10px;
            border-radius: 5px;
            white-space: pre-wrap;
            word-wrap: break-word;
            font-size: 12px;
        }

        .alert {
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            color: #856404;
        }
    </style>
</head>

<body>
    <div class="container">
        <a href="/" class="back-link">← Back to Dashboard</a>
        <div class="header">
            <h1>PrintNode Webhook Status</h1>
            <p style="color: #666; margin-top: 5px;">Hiển thị các sự kiện nhận được từ PrintNode.</p>
        </div>

        <% if (!webhookConfigured) { %>
            <div class="alert">
                ⚠️ <strong>Warning:</strong> PRINTNODE_WEBHOOK_SECRET chưa được cấu hình trong file .env. Webhook sẽ
                không hoạt động.
            </div>
            <% } %>

                <div class="jobs-section">
                    <div class="jobs-header">
                        <h2>Received Events</h2>
                    </div>

                    <% if (events.length===0) { %>
                        <div class="empty-state">
                            <p>Chưa nhận được sự kiện nào từ PrintNode.</p>
                        </div>
                        <% } else { %>
                            <table class="jobs-table">
                                <thead>
                                    <tr>
                                        <th>Timestamp</th>
                                        <th>Event Type</th>
                                        <th>Details</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% events.forEach(event=> { %>
                                        <tr>
                                            <td style="vertical-align: top; width: 200px;">
                                                <%= new Date(event.timestamp).toLocaleString('vi-VN') %>
                                            </td>
                                            <td style="vertical-align: top; width: 150px;"><strong>
                                                    <%= event.event %>
                                                </strong></td>
                                            <td>
                                                <pre><%= JSON.stringify(event, null, 2) %></pre>
                                            </td>
                                        </tr>
                                        <% }); %>
                                </tbody>
                            </table>
                            <% } %>
                </div>
    </div>
</body>

</html>